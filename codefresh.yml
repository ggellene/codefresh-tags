version: '1.0'
steps:
  FinalBuild:
    title: Building Testable Docker Image
    type: build
    image_name: parkhubprime/test-${{CF_REPO_NAME}}
    tag: CI-${{CF_BRANCH_TAG_NORMALIZED}}
    dockerfile: Dockerfile.test
